{
  "name": "@simracing/desktop",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "concurrently -k -n build,app -c yellow,cyan \"tsc -p tsconfig.json -w\" \"wait-on dist/main.js && electron dist/main.js\"",
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'no lint configured'",
    "format": "echo 'no format configured'"
  },
  "dependencies": {
    "@simracing/config": "workspace:*",
    "electron": "^31.2.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.5.4",
    "wait-on": "^7.2.0",
    "@types/node": "^20.14.10",
    "@types/electron": "^1.6.12"
  }
}
